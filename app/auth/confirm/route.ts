import {NextResponse} from "next/server";import {createServerClient} from "@supabase/ssr";import {cookies} from "next/headers";
export async function GET(req:Request){const url=new URL(req.url),code=url.searchParams.get("code"),next=url.searchParams.get("next")||"/dashboard";if(code){const store=await cookies();const supabase=createServerClient("https://eyboryymmcpwspjxidmk.supabase.co","sb_publishable_n4GU_D-91B2YxLt3O6JqLA_QrwHUChF",{cookies:{getAll(){return store.getAll()},setAll(cs){cs.forEach(({name,value,options})=>store.set(name,value,options))}}});await supabase.auth.exchangeCodeForSession(code)}return NextResponse.redirect(new URL(next,url.origin))}