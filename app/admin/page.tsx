"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase";
export default function Admin(){const s=createClient(),[ok,setOk]=useState(false),[sellers,setSellers]=useState<any[]>([]),[offers,setOffers]=useState<any[]>([]),[msg,setMsg]=useState("");async function load(){const{data:{user}}=await s.auth.getUser();if(!user){location.href="/admin/login";return}const{data:p}=await s.from("profiles").select("role").eq("id",user.id).single();if(p?.role!=="admin"){setMsg("Access denied: this account is not an admin.");return}setOk(true);const[{data:a},{data:b}]=await Promise.all([s.from("sellers").select("*").order("created_at",{ascending:false}),s.from("seller_offers").select("*,sellers(business_name),drops(slug,products(brand,name))").order("created_at",{ascending:false})]);setSellers(a||[]);setOffers(b||[])}useEffect(()=>{load()},[]);async function seller(id:string,status:string){await s.from("sellers").update({status}).eq("id",id);load()}async function offer(id:string,status:string){await s.from("seller_offers").update({status}).eq("id",id);load()}if(!ok)return <div className="authShell"><div className="authCard"><h1>TILT↘ Admin</h1><p>{msg||"Verifying admin access…"}</p>{msg&&<a className="btn" href="/admin/login">ADMIN LOGIN</a>}</div></div>;return <div className="wrap"><nav className="nav"><a className="logo" href="/">TILT↘ CONTROL</a><span className="tag">ADMIN</span></nav><section className="section"><h1>Seller approvals</h1><div className="card"><table className="table"><tbody>{sellers.map(x=><tr key={x.id}><td><b>{x.business_name}</b><br/><small>{x.gstin||"GSTIN pending"} · {x.city}</small></td><td>{x.status}</td><td><button className="btn lime" onClick={()=>seller(x.id,"approved")}>APPROVE</button> <button className="pill" onClick={()=>seller(x.id,"rejected")}>REJECT</button></td></tr>)}</tbody></table></div></section><section className="section"><h1>Seller bids</h1><div className="card"><table className="table"><tbody>{offers.map(x=><tr key={x.id}><td><b>{x.sellers?.business_name}</b><br/><small>{x.drops?.products?.brand} {x.drops?.products?.name}</small></td><td><b>₹{Number(x.unit_price).toLocaleString("en-IN")}</b><br/>{x.quantity} units</td><td>{x.status}</td><td><button className="btn lime" onClick={()=>offer(x.id,"accepted")}>ACCEPT</button> <button className="pill" onClick={()=>offer(x.id,"rejected")}>REJECT</button></td></tr>)}</tbody></table></div></section></div>}