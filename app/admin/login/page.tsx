"use client";import{useState}from"react";import{createClient}from"@/lib/supabase";export default function AdminLogin(){const[e,setE]=useState(""),[m,setM]=useState("");async function go(){const{error}=await createClient().auth.signInWithOtp({email:e,options:{emailRedirectTo:location.origin+"/admin"}});setM(error?.message||"Secure login link sent. Admin role is verified after sign-in.")}return <div className="authShell"><div className="authCard"><span className="tag">TILT CONTROL ROOM</span><h1>Admin access.</h1><input className="input" type="email" placeholder="admin@email.com" value={e} onChange={x=>setE(x.target.value)}/><button className="btn full" onClick={go}>SECURE LOGIN →</button>{m&&<p>{m}</p>}</div></div>}