"use client";
import {useEffect,useState} from "react";import AdminShell from "../AdminShell";import LoadingOverlay from "../../LoadingOverlay";import {useAdminGuard} from "../useAdminGuard";
export default function OrdersAdmin(){const{s,ready,error}=useAdminGuard();const[rows,setRows]=useState<any[]>([]);
useEffect(()=>{if(!ready)return;(async()=>{const{data}=await s.from("orders").select("*").order("created_at",{ascending:false});setRows(data||[])})()},[ready]);
if(error)return <div className="authShell"><div className="authCard"><p>{error}</p></div></div>;if(!ready)return <LoadingOverlay label="Verifying admin…"/>;
return <AdminShell title="Orders" subtitle="Track unlocked buyer orders and seller-payment references."><section className="adminPanel"><div className="adminPanelHead"><div><span className="sectionKicker">ORDERS</span><h2>Buyer orders</h2></div><b>{rows.length}</b></div><div className="adminTableWrap"><table className="table"><thead><tr><th>Order</th><th>Amount</th><th>Status</th><th>Seller payment</th></tr></thead><tbody>{rows.length?rows.map(x=><tr key={x.id}><td><b>{String(x.id).slice(0,8).toUpperCase()}</b><br/><small>{new Date(x.created_at).toLocaleString("en-IN")}</small></td><td><b>₹{Number(x.final_price||0).toLocaleString("en-IN")}</b><br/><small>Balance ₹{Number(x.balance_due||0).toLocaleString("en-IN")}</small></td><td><span className="tag">{x.status||"—"}</span><br/><small>{x.payment_status||"unpaid"}</small></td><td>{x.balance_payment_ref?<><b>{x.balance_payment_ref}</b><br/><small>{x.balance_payment_mode||"—"}</small></>:<small>No reference submitted</small>}</td></tr>):<tr><td colSpan={4}>No orders yet.</td></tr>}</tbody></table></div></section></AdminShell>}
