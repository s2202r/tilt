import {createClient} from "@supabase/supabase-js";import {notFound} from "next/navigation";import DropClient from "./DropClient";
const sb=createClient("https://eyboryymmcpwspjxidmk.supabase.co","sb_publishable_n4GU_D-91B2YxLt3O6JqLA_QrwHUChF");
export const revalidate=20;
export default async function DropPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const{data,error}=await sb.from("drops").select("id,slug,status,starts_at,ends_at,products(id,brand,name,sku,reference_price,image_url),drop_tiers(id,buyer_threshold,price)").eq("slug",slug).single();if(error||!data)return notFound();const{data:stats}=await sb.rpc("get_drop_public_stats",{p_drop_id:data.id});return <DropClient drop={data as any} buyers={Number(stats?.[0]?.committed_count||0)}/>} 