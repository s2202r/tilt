"use client";import {useState} from "react";
const commitment=(price:number)=>Math.round((price*.01)/50)*50;\nexport default function Drop(){const [target,setTarget]=useState("59999");const token=commitment(Number(target)||0);const tiers=[["50","₹64,999"],["100","₹62,999"],["250","₹59,999"],["500","₹57,999"]];return <div className="wrap"><nav className="nav"><a href="/" className="logo" style={{color:"inherit",textDecoration:"none"}}>TILT↘</a><a className="pill" href="/dashboard">My TILTs</a></nav><section className="section" style={{maxWidth:850,margin:"auto"}}><span className="tag">🔥 LIVE · 2d 14h left</span><h1 style={{fontSize:56,letterSpacing:"-.05em",marginBottom:8}}>Samsung 55″ OLED TV</h1><p className="old">Reference market price ₹69,990</p><div className="card" style={{margin:"28px 0"}}><div className="eyebrow">Current unlocked price</div><div className="price" style={{fontSize:64}}>₹59,999</div><div className="bar"><i style={{width:"57%"}}/></div><div className="meta"><b>287 committed buyers</b><span>213 more → ₹57,999</span></div></div><h2>At what price will you actually buy?</h2><div className="grid">{tiers.map(([n,p])=><button key={n} className="card" onClick={()=>setTarget(p.replace(/[₹,]/g,""))} style={{textAlign:"left",outline:target===p.replace(/[₹,]/g,"")?"3px solid #111":"none"}}><b>{p}</b><br/><small>{n}+ buyers</small></button>)}</div><div className="card" style={{marginTop:20}}><label><b>My maximum buy price</b></label><div style={{display:"flex",gap:10,marginTop:10}}><span className="pill">₹</span><input value={target} onChange={e=>setTarget(e.target.value)} style={{flex:1,fontSize:24,border:"1px solid #aaa",borderRadius:14,padding:12}}/></div><p>Commitment token: <b>₹{token.toLocaleString("en-IN")}</b> <small>(1% of your committed price, rounded to nearest ₹50)</small> · adjusted against your final purchase. If your chosen price is not unlocked, the commitment is released/refunded under the drop terms.</p><button className="btn lime" style={{width:"100%",fontSize:18}}>COMMIT ₹{token.toLocaleString("en-IN")}</button></div></section></div>}