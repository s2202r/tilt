"use client";import{useState}from"react";import{createClient}from"@/lib/supabase";export default function SellerLogin(){const[e,setE]=useState(""),[m,setM]=useState("");async function go(){const{error}=await createClient().auth.signInWithOtp({email:e,options:{emailRedirectTo:location.origin+"/seller/profile"}});setM(error?.message||"Seller login link sent.")}return <div className="authShell"><a className="logo" href="/">TILT↘</a><div className="authCard"><span className="tag">SELLER DESK</span><h1>Demand is waiting.</h1><p>Login, complete verification, then bid on live demand.</p><input className="input" type="email" placeholder="business@email.com" value={e} onChange={x=>setE(x.target.value)}/><button className="btn lime full" onClick={go}>SELLER LOGIN →</button>{m&&<p>{m}</p>}</div></div>