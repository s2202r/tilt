"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase";
export default function Seller(){const s=createClient();const[ready,setReady]=useState(false);const[profile,setProfile]=useState<any>(null);useEffect(()=>{(async()=>{const{data:{user}}=await s.auth.getUser();if(!user){location.replace("/seller/login");return}const{data}=await s.from("sellers").select("*").eq("owner_id",user.id).maybeSingle();if(!data){location.replace("/seller/profile");return}setProfile(data);setReady(true)})()},[]);if(!ready)return <div className="authShell"><div className="authCard"><h1>TILT↘ Seller</h1><p>Checking seller access…</p></div></div>;return <div className="dash"><aside className="side"><div className="logo">TILT↘</div><p>SELLER</p><a className="active">Live demand</a><a href="/seller/bids">My offers / Bid</a><a>Orders</a><a href="/seller/profile">Verification</a><a href="/">← Store</a></aside><main className="main"><span className="tag">{profile.status==="approved"?"✓ VERIFIED SELLER":"APPROVAL "+String(profile.status||"pending").toUpperCase()}</span><h1>Demand, before discount.</h1><p>Seller demand data is private. Approved sellers can open live drops and submit bids.</p><div className="card"><h2>{profile.business_name}</h2><p>{profile.status==="approved"?"You are approved to bid on active TILTs.":"Your seller account must be approved by admin before bidding."}</p><a className="btn lime" href="/seller/bids">OPEN BIDDING DESK →</a></div></main></div>}