"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase";
export default function Dashboard(){const s=createClient();const[ready,setReady]=useState(false);useEffect(()=>{(async()=>{const{data:{user}}=await s.auth.getUser();if(!user){location.replace("/login");return}setReady(true)})()},[]);if(!ready)return <div className="authShell"><div className="authCard"><h1>TILT↘</h1><p>Checking your account…</p></div></div>;return <div className="dash"><aside className="side"><div className="logo">TILT↘</div><a className="active">My TILTs</a><a>Orders</a><a>Watchlist</a><a href="/profile">Profile</a><a href="/">← Store</a></aside><main className="main"><h1>My TILTs</h1><div className="card"><p>Your real commitments, unlocked prices and orders will appear here.</p><p className="tiny">Preview metrics have been removed from the private buyer area.</p></div></main></div>}